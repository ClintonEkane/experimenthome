import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full widget tests require Firebase initialisation.
    // Integration tests are handled via the running app.
    expect(true, isTrue);
  });
}
